/**
 * TradeRetro product identity.
 *
 * Single source for name, version, and release title so the whole
 * application reports one consistent version string. v0.9.0 is the
 * intentional capstone release-candidate version (v1.0.0 is reserved
 * until the release process is complete).
 */
export const PRODUCT = {
  name: 'TradeRetro',
  version: 'v0.9.0',
  releaseTitle: 'Capstone Release Candidate',
};

export default PRODUCT;