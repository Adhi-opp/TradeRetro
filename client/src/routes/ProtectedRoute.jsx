/**
 * Placeholder protected route boundary.
 *
 * TradeRetro currently has no frontend auth gate. This component preserves the
 * future extension point without changing current navigation behavior.
 */
export default function ProtectedRoute({ children }) {
  return children;
}
