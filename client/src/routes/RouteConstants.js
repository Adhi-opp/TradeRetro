/**
 * Route constants for future URL-based routing.
 *
 * The app currently uses in-memory navigation, so these constants document
 * intent without changing browser URLs.
 */
export const ROUTES = {
  LANDING: 'landing',
  DASHBOARD: 'dashboard',
};

export default ROUTES;
